import { Province } from "./province.model";

export class Map {
  //id: number;
  province: Province;
  maxHeight: number;
  minHeight: number;
  distMax: number;
  latMax: number;
  latMin: number;
  lonMax: number;
  lonMin: number;

}